version: '3'

services:
  indexer:
    image: "dwh_indexer:latest"
    volumes:
      - idx-data:/root
    restart: unless-stopped
    networks:
      - dwh-tier

networks:
  dwh-tier:
    external:
      name: dwh-network